import { SortBookTitlePipe } from './sort-book-title.pipe';

describe('SortBookTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new SortBookTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
